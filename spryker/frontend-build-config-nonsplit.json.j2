{
  "path": "assets/%SPRYKER_BUILD_HASH%/%theme%",
  "namespaces": [
{% for store in stores %}
    {
      "codeBucket": "{{ store }}",
      "namespace": "{{ store }}",
      "themes": [],
      "defaultTheme": "default"
{% if loop.last %}    } {% else %}    }, {% endif %}
{% endfor %}
  ]
}
